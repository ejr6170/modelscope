import Dashboard from "./components/Dashboard";

// Ghost HUD — renders the Dashboard with useState, useEffect, useMemo, useCallback, AnimatePresence
export default function Home() {
  return <Dashboard />;
}
